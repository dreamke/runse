# 2026-06-23

## 创建文本润色工具 polish.html

- 单文件 HTML 工具，无后端依赖
- 三栏布局：左侧输入原文 / 中间选择5种润色风格 / 右侧显示结果
- 5种预设风格：微信对话体、公文请示体、正式报告体、英文翻译体、口语化简化
- 支持 OpenAI / Anthropic / 自定义 API，配置存 localStorage
- 流式 SSE 输出，支持中途停止
- 撤回重做（最多20步历史），一键复制，差异对比高亮（词级别 LCS diff）
- 黑白灰极简设计，禁用渐变/圆角卡片/AI图标
- 文件路径：D:\workbuddy\runse\polish.html

## 修复 polish.html：硅基流动支持 + CORS 修复（19:00）

- 新增硅基流动为默认 API 类型，内置 base `https://api.siliconflow.cn`，默认 `Qwen/Qwen3-8B`
- 访问地址：http://localhost:8899/polish.html

## 修正：硅基流动原生 CORS，移除冗余逻辑（19:05）

- 移除 HTTP 服务器相关说明和降级 fallback 代码（硅基流动 API 本身支持 CORS，file:// 协议也可直接调）
- 简化 callOpenAICompat：纯流式 fetch，无 try/catch 降级包装
- 删除 callOpenAIFallback 函数
- 说明改为"硅基流动原生 CORS，可双击 HTML 使用"

## 设置面板扩展：API测试 + 风格模板管理（19:08）

- 新增「测试连接」按钮：用当前配置发 min token 请求验证 API Key 和地址
- 新增「风格模板」Tab：列表编辑名称/描述/System Prompt，实时自动保存
- 每个风格支持"恢复默认"（重置为 DEFAULT_STYLES）和"删除"
- 底部「+ 添加风格」创建自定义模板
- 模板数据持久化到 localStorage（polish_styles）
- 设置面板改为 Tab 切换：API设置 / 风格模板
- 样式风格编辑后同步刷新侧栏按钮

## 设置面板布局修复（19:12）

- 修复 modal-head 内 Tabs 标题被挤换行问题
- 将「设置」标题与 Tabs 拆为两行：标题独占一行，Tabs 独占一行
- tab-btn 加 min-width:0 + white-space:nowrap 防止内容溢出换行
- modal-head 固定高度 44px，标题和关闭按钮水平居中

## 标题副标题（19:15）

- 标题后追加灰色小字 "v0.1 by Daydream"（移动端隐藏）

## 移动端适配（19:34）

- 断点 ≤768px 进入手机布局
- 顶部 mobile-bar：三个 Tab（原文/风格/结果）切换显示
- 三栏改为上下堆叠：默认展开「结果」栏，原文和风格可点击 col-head 折叠
- 风格按钮：手机端改为两列横排
- 头部：移动端隐藏撤回/重做按钮、隐藏副标题
- 设置弹窗：移动端全屏化
- iOS 输入框 14px 字号避免自动放大

## 移动端 v2：取消折叠（19:38）

- 取消 Tab 切换和折叠机制
- 手机端三栏全部同时显示，改为纵向堆叠
- 原文：固定最小高度 180px
- 风格：自动高度，按钮两列横排
- 结果：占满剩余空间
- mobile-bar 简化为标题条
